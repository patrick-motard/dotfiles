# -*- mode: sh; -*-
# vim: set ft=zsh:
#region homebrew
# Use brew shellenv for proper homebrew PATH setup
eval "$(/opt/homebrew/bin/brew shellenv)"
#endregion homebrew

{{ if eq .chezmoi.hostname "GVXPDWWKWG" -}}
#BEGIN ZETUP
eval "$( zetup env shell-exports --zsh )"
#END ZETUP

## BEGIN -- managed by zendesk:workstation -- ##
[[ -f ~/.local/bin/zendesk_zprofile.sh ]] && source ~/.local/bin/zendesk_zprofile.sh
## END -- managed by zendesk:workstation -- ##

export ZDP_DBT_DIR={{ .chezmoi.homeDir }}/zdp_dbt_repos
{{ end -}}
