#!/usr/bin/env bash
# Used by lastpass-cli, setup by lastpass role.
# See "Agent" section in lpass man page (man lpass).
# When set to 0, the agent never times out (never asks more than once for password).
export LPASS_AGENT_TIMEOUT=0
